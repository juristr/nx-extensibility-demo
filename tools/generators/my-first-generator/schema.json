{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "my-first-generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Conference name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What's the name of the conference?"
    }
  },
  "required": ["name"]
}
